#include <stdio.h>
#include <stdlib.h>

/**
 * add - add two integers together
 * @a: first integer
 * @b: second integer
 * Return: return addition result
 */
int add(int a, int b)
{
	return (a + b);
}

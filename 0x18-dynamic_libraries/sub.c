#include <stdlib.h>
#include <stdio.h>

/**
 * sub - subtract two integers together
 * @a: first integer
 * @b: second integer
 * Return: return subtracted result
 */
int sub(int a, int b)
{
	return (a - b);
}
